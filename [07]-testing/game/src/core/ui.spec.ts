import { vi } from 'vitest';
import * as motor from './motor';
import { orderCard } from './ui';

describe('UI specs', () => {
  describe('orderCard specs', () => {
    it('', () => {
      // Arrange
      // Act
      // Assert
    });
  });
});
